# Consolidated requirements for the ollama/juridico-review-ai workspace
# Install the core runtime dependencies used across the project.
# Some packages are optional depending on which features you use (LLM backend, FAISS, GPU, etc.).

# Web UI
streamlit>=1.19.0

# LLM / frontier providers
# Use the PyPI package name that provides `google.generativeai` module
# (google-generative-ai is the correct package name on PyPI)
google-generative-ai>=0.8.5
openai>=1.0.0
anthropic>=0.7.0
ollama>=0.1.0

# HTTP
requests>=2.28.0

# Data and reports
pandas>=2.0.0
openpyxl>=3.1.0
python-docx>=0.8.11
pyyaml>=6.0.1

# PDF parsing
pdfplumber>=0.9.0
PyPDF2>=3.0.0
pymupdf>=1.23.0

# Retrieval / embeddings / ranking
numpy>=1.24.0
sentence-transformers>=2.2.0
torch>=2.0.0
faiss-cpu>=1.7.4
rank-bm25>=0.2.2
rapidfuzz>=3.0.0
scikit-learn>=1.3.0

# Utilities
click>=8.1.0
python-dotenv>=1.0.0
pydantic>=2.0.0
jinja2>=3.1.0
rich>=13.0.0

# Dev / testing
pytest>=7.4.0

# Optional: if you don't need some features, comment/remove corresponding lines.
